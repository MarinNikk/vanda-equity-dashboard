"""Pydantic request/response schemas — the API's data shapes.

These define the contract the HTTP layer speaks. They contain no logic and no
SQL; routers and services share them as a common vocabulary.
"""
